# ============================================================
# Medical Chatbot RAG v2.0.0 - Serverless Dependencies
# ============================================================
# NO local model downloads. All embeddings/LLMs are API-based.
# Target install size: <50MB (down from ~1.75GB)
# ============================================================

# === LangChain Core ===
langchain>=1.3.0,<2.0.0
langchain-core>=1.4.0,<2.0.0
langchain-community>=0.4.0,<1.0.0
langchain-openai>=1.3.0,<2.0.0
langchain-groq>=1.1.0,<2.0.0
langchain-pinecone>=0.2.0,<0.3.0

# === Embeddings (API-based, NO local models) ===
# langchain-huggingface is used ONLY for HuggingFaceEndpointEmbeddings
# (API calls). It does NOT pull in sentence-transformers or torch.
langchain-huggingface>=0.1.0
huggingface-hub>=0.20.0

# === Vector Database ===
pinecone>=7.0.0,<8.0.0

# === Database ===
Flask-SQLAlchemy>=3.1.0,<4.0.0
psycopg2-binary>=2.9.9

# === Web Framework ===
Flask>=3.0.0,<4.0.0
Flask-Login>=0.6.0,<1.0.0
Flask-Bcrypt>=1.0.0,<2.0.0
Flask-Limiter>=3.5.0,<4.0.0

# === Utilities ===
python-dotenv>=1.0.0
requests>=2.31.0
email-validator>=2.0.0

# === PDF Processing (for store_index.py / indexing only) ===
pypdf>=4.0.0
langchain-text-splitters>=0.3.0